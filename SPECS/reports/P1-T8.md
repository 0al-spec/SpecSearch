# P1-T8: Docker delivery and MVP exit

Decision: PARTIAL. The local discovery prototype is delivered; human quality
acceptance is not complete. Both evaluation reports retain quality_accepted=false.
Ollama passes provisional numeric test targets; LM Studio fails negative strong
matches. No test labels were used for tuning. See P1-T7.md for measured retrieval.

## Delivered

- API, CLI, BM25/vector/hybrid search, exact verification and comparison UI.
- Docker Compose binds only 127.0.0.1:8031; embedding services stay on the host.
- Non-root app user, read-only root/config/corpus, writable named data volume,
  dropped capabilities, bounded tmpfs and healthcheck.
- Versioned compiled TypeScript bundle with CI rebuild drift check; frozen Python
  dependencies and pinned SpecPM validator. UI attribution/license included.
- Read-only registry integration and repeatable import/evaluation instructions.
- Status includes source counts, snapshot age and stale indication. Snippets link
  to the complete stored field and evidence references, rendered as inert text.

## Local evidence (2026-09-06)

Docker imported 99 records: 89 candidates and 10 registry versions, 289 documents.
Active snapshot: 60639714c9e54cca8d00e2de402ddb52. Restart preserved this snapshot
and all 99 records. Source validation copies are temporary; originals and registry
were not changed. No package code or archive downloads were executed.

Commands actually run:

- make check: Ruff lint/format passed; 36 tests passed, one optional corpus test
  skipped; coverage 93.14%.
- SPECSEARCH_TEST_CORPUS=<frozen-bundle> .venv/bin/pytest --cov=specsearch
  --cov-fail-under=90 -q: 37 passed, coverage 93.25%; two dependency deprecations.
- npm run build: passed, compiled bundle produced.
- SPECSEARCH_BASE_URL=http://127.0.0.1:8031 npm run test:e2e: three passed,
  including 1440px/390px search, source field, comparison, verification and hostile
  text. Desktop/mobile screenshots retained locally under .data/screenshots.
- docker compose build and up --force-recreate --wait: healthy.
- docker compose restart followed by make dev-smoke: same snapshot, registry and
  candidate exact verification passed, hybrid passed without fallback.
- docker inspect: app user; root read-only; corpus/config mounts read-only.

Warm synthetic 1000-package p95 was 217ms (20 queries). This tests performance,
not corpus quality. Cold model startup and uncached indexing remain unmeasured.
The real corpus is small and n8n-heavy; results cannot establish general utility.

## Next bounded step

Review relevance labels and a more diverse frozen holdout before declaring MVP
quality accepted. Keep Ollama as the provisional default. Investigate LM Studio
hard-negative calibration only on dev data. Do not expand to automatic scraping,
publication, runtime matching or a separate vector database without new evidence.
