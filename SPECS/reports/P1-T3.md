# P1-T3: Lexical baseline

SQLite FTS5/BM25, exact package/version/capability/intent lookups, Unicode and
compound identifier tokens, pre-retrieval filters and top-50 document grouping.
Immutable snapshots survive replacement; failed builds preserve active pointer.

Validation: pytest -q: 13 passed, 1 local-corpus test skipped without environment
opt-in. Ruff check/format passed. No semantic retrieval quality claim.
