# P1-T5: Hybrid service

CLI import/build/search/status/serve and read-only search/package/verify/status
API. Exact ID path, cosine + BM25 package RRF, explicit degraded fallback,
non-probabilistic scores, source fields and fresh verification. Local-only
Host/Origin guards, JSON/body bounds and no raw query access log.

Validation: 18 tests passed, 1 optional corpus skip; Ruff passed. Ollama live
index: 89 packages, 279 documents. Exact RTK lookup succeeded. Russian smoke
query completed in 144ms but ranked n8n ahead of the intended new RTK candidate.
This is a visible retrieval-quality limitation, not an implementation pass for
the quality gate. P1-T7 must measure the frozen queries before any exit claim.
Two dependency deprecation warnings from FastAPI/Starlette tests remain visible.
