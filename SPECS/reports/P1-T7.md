# P1-T7: Evaluation and hardening

All 40 frozen queries measured in lexical, vector and hybrid modes for both
providers. Dev-only thresholds retain provider/corpus/query bindings. Labels
remain provisional; quality_accepted=false for both providers. No test tuning.

| Test metric | Ollama hybrid | LM Studio hybrid |
| --- | ---: | ---: |
| Recall@5 RU / EN | 1.00 / 1.00 | 1.00 / 1.00 |
| nDCG@5 RU / EN | .954 / 1.00 | 1.00 / 1.00 |
| Negative strong rate RU / EN | 0 / 0 | 0 / .333 |
| Overall nDCG@5 | .977 | 1.00 |
| Overall BM25 nDCG@5 | .962 | .962 |
| Warm actual-corpus hybrid p95 | 57ms | 261ms |

The test split has only eight positive queries per language (including three
exact IDs) and three negatives per language. Removing exact-ID cases, hybrid
Recall@5 is 1.00 for both; RU nDCG@5 is .926 for Ollama and 1.00 for LM Studio.
This small, agent-labeled and n8n-heavy corpus does not establish general quality.
The earlier dev RTK Russian smoke failure remains in the observed history.
LM Studio fails the negative strong-match target. Ollama meets numeric targets
on provisional test labels, not human acceptance. Default provider stays Ollama.

Synthetic replication to 1000 packages / 3144 documents measured warm p95 217ms
across 20 queries; not a quality or cold-model measurement. Cache-warm index
build was .82s; a real new-corpus embedding build would cost more.

Live SpecPM Docker make dev-reload passed. Ten registry versions imported with
zero errors after adding only the exact same-origin trailing-slash redirect.
Captured real /v0 payload fixtures are validated with pinned SpecPM validation.
No archive downloads, package execution, registry changes or source edits.

Validation: full explicit-corpus pytest coverage gate >=90%; Ruff passed.
Coverage includes real local validation, lifecycle/filtering, pointer rollback,
provider failure/mismatch, hostile YAML, path escape, body/Origin guards and
dev/test separation. Detailed counts are in the final delivery report.

Follow-up: maintainer review of labels, larger diverse corpus, additional hard
negatives and cross-language queries without English technology anchors.
