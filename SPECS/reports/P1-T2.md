# P1-T2: Safe import

Read-only bounded snapshots, alias/depth/size guards, no-follow file descriptors,
pinned SpecPM validation, field-based documents and separate candidate identities.
Registry metadata uses the pinned SpecPM payload validator; no archive download.

Validation: explicit local 89-directory corpus import passed with zero rejected
packages; 11 tests passed. Ruff check/format passed. Registry live integration
remains for the Docker/evaluation stage; no registry availability claim yet.
